import {chromium} from '@playwright/test';import fs from 'node:fs';fs.mkdirSync('test-results/visual',{recursive:true});const browser=await chromium.launch();for(const [label,viewport,scheme,lang] of [['desktop',{width:1440,height:1000},'dark','en'],['mobile',{width:393,height:851},'dark','en'],['khmer',{width:393,height:851},'light','km']]){const context=await browser.newContext({viewport,colorScheme:scheme});const page=await context.newPage();await page.goto('http://127.0.0.1:5173/');if(lang==='km')await page.getByRole('button',{name:'Language',exact:true}).click();await page.locator('.game-card').first().waitFor();await page.screenshot({path:'test-results/visual/home-'+label+'.png',fullPage:true});for(const game of ['temple-tower','tuk-tuk-rush']){if(lang==='km')continue;await page.goto('http://127.0.0.1:5173/play/'+game);await page.getByRole('button',{name:'Let’s play',exact:true}).click();await page.waitForTimeout(500);await page.locator('.game-frame').screenshot({path:'test-results/visual/'+game+'-'+label+'.png'});}await context.close()}await browser.close();console.log('Visual screenshots saved.');
