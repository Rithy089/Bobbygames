import {render,screen,fireEvent,cleanup} from '@testing-library/react';import {afterEach,describe,it,expect} from 'vitest';import {MemoryRouter} from 'react-router-dom';import '../src/i18n';import GameCard from '../src/components/GameCard';import Account from '../src/pages/Account';import {games} from '../src/lib/catalog';import {usePortal} from '../src/lib/store';afterEach(cleanup);describe('portal components',()=>{it('favorites remain accessible and toggle independently of navigation',()=>{usePortal.setState({favorites:[]});render(<MemoryRouter><GameCard game={games[0]}/></MemoryRouter>);const heart=screen.getByRole('button',{name:'Favorite Mango Catch'});expect(heart).toHaveAttribute('aria-pressed','false');fireEvent.click(heart);expect(heart).toHaveAttribute('aria-pressed','true');expect(screen.getByRole('link',{name:'Play now'})).toHaveAttribute('href','/play/mango-catch')});it('keeps guest play available when authentication is unconfigured',()=>{render(<MemoryRouter initialEntries={['/login']}><Account/></MemoryRouter>);expect(screen.getByText(/Accounts are not connected yet/)).toBeInTheDocument();expect(screen.getByRole('button',{name:'Log in'})).toBeDisabled();expect(screen.getByRole('link',{name:'Free to play'})).toHaveAttribute('href','/games')})});
