# September arcade update

## Inspection and scope
- Clean working tree at `0019243`; no AGENTS.md in the workspace or its parent directories. Preserve existing routes, persisted guest data and account integration.
- GitHub API confirms `Rithy089/Bobbygames` is the public BobbyGames project repository. The supplied source-code URL is absent from this checkout, so add it as a separate constant/link; retain Bobby's personal profile and portfolio URLs.
- Discovery currently has four icon-only cards. Add topic-matched original illustrations, explicitly labeled as illustrations, plus city/countryside/food/market content with primary-source citations.
- Mango pointer movement currently teleports; feedback is mostly a full-screen flash. Improve smoothing, swept catch tests and local feedback.
- Tower blocks appear instantly and perfect feedback is very brief. Add restrained placement settling, greener sunset scenery and clear feedback without altering fair overlap rules.
- Rush collision uses target lane instead of the visible vehicle position. Align collision with visual movement, retain generous boundaries, add street/vehicle detail.
- Native Canvas remains appropriate for arcade games. Market Match will use semantic DOM buttons and a pure state machine for accessible hidden cards and reliable rapid input.

## Ordered acceptance
1. Discovery: all eight topics have optimized matching illustrations, reserved aspect ratios, bilingual alt text and source links; no generated image called a photograph.
2. Arcade polish: improved actual gameplay art/feedback, smooth controls, deterministic collision tests, reduced decorative motion, persistent sound, pause/cleanup retained.
3. Market Match: 6/8/12 pairs; all three difficulties complete; moves first then active completion time; independent local bests; race-safe flips/restarts; keyboard/touch; catalog/favorites/recent integration. No global score submission for this new local-only game.
4. Verification: strict types, lint, rules/components, desktop/mobile Playwright, screenshots, production build, image/link checks and source secret scan. Record failures and corrections honestly.

## Deferred
Supabase provisioning, hosted auth/email and global score rollout remain explicitly deferred. Preserve honest unavailable states. All new Khmer strings are drafts listed in KHMER-REVIEW.md. No additional games or paid services.
