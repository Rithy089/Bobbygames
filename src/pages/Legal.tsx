import {useTranslation} from 'react-i18next';import {portfolio} from '../lib/catalog';export default function Legal({kind}:{kind:'privacy'|'terms'}){const {t}=useTranslation();return <article className="prose legal"><span className="eyebrow">BOBBYGAMES</span><h1>{t(kind)}</h1><p>{t(kind+'Text')}</p>{kind==='privacy'&&<p>{t('privacyContact')}</p>}<a className="text-link" href={portfolio} target="_blank" rel="noopener noreferrer">{t('contact')} → {t('portfolio')}</a></article>}
