import {createLoop} from '../shared/loop';import {freshSnapshot,type GameOptions,type Scene,W} from '../shared/types';import {basket,countryside,fruit} from '../shared/draw';import {catchItem,missItem,difficulty,config,type MangoState} from './rules';
export default function createGame(options:GameOptions){let state:MangoState={score:0,lives:3,combo:0,streak:0};let x=W/2,spawn=.5,flash=0;let items:{x:number;y:number;type:'mango'|'dragon'|'stone';spin:number}[]=[];const snapshot=freshSnapshot();
const scene:Scene={snapshot,reset(){Object.assign(snapshot,freshSnapshot());state={score:0,lives:3,combo:0,streak:0};x=W/2;spawn=.5;items=[];flash=0},update(dt,input){const d=difficulty(snapshot.elapsed);if(input.left||input.right){x+=(Number(input.right)-Number(input.left))*520*dt;input.pointer=null}else if(input.pointer!==null)x=input.pointer;x=Math.max(55,Math.min(W-55,x));spawn-=dt;flash=Math.max(0,flash-dt);if(spawn<=0){spawn=d.spawn;items.push({x:45+Math.random()*(W-90),y:-30,type:Math.random()<d.hazardChance?'stone':Math.random()<.2?'dragon':'mango',spin:Math.random()*2})}for(const item of items){item.y+=d.speed*dt;item.spin+=dt;if(item.y>=config.catchY-20&&item.y<=config.catchY+15&&Math.abs(item.x-x)<55){state=catchItem(state,item.type==='stone');options.audio(item.type==='stone'?'miss':'catch');if(item.type==='stone')flash=.2;item.y=1000}else if(item.y>570&&item.y<900){state=missItem(state,item.type==='stone');if(item.type!=='stone'){options.audio('miss');flash=.2}item.y=1000}}items=items.filter(i=>i.y<900);Object.assign(snapshot,{score:state.score,lives:state.lives,combo:state.combo});if(state.lives<=0||snapshot.elapsed>=config.maxSeconds)snapshot.phase='over'},draw(ctx){countryside(ctx);for(const item of items)fruit(ctx,item.x,item.y,item.type,Math.sin(item.spin)*.3);basket(ctx,x,config.catchY);if(state.combo>1){ctx.fillStyle='#fff3cd';ctx.font='bold 21px Inter, sans-serif';ctx.textAlign='center';ctx.fillText('×'+state.combo,x,config.catchY-52)}if(flash>0){ctx.fillStyle='rgba(188,55,55,.16)';ctx.fillRect(0,0,800,600)}}};return createLoop(scene,options)}
