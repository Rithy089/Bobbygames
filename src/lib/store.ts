import {create} from 'zustand';import {persist} from 'zustand/middleware';import type {GameId} from './catalog';
export type Theme='light'|'dark'|'system';export type AudioSettings={sound:boolean;music:boolean;effects:boolean;volume:number};
export type Run={game:GameId;score:number;duration:number;date:string};
type Portal={language:'en'|'km';theme:Theme;audio:AudioSettings;favorites:GameId[];recent:GameId[];best:Partial<Record<GameId,number>>;runs:Run[];setLanguage:(v:'en'|'km')=>void;setTheme:(v:Theme)=>void;setAudio:(v:Partial<AudioSettings>)=>void;toggleFavorite:(id:GameId)=>void;visit:(id:GameId)=>void;finish:(run:Run)=>void};
export const usePortal=create<Portal>()(persist((set)=>({language:document.documentElement.lang==='km'?'km':'en',theme:'system',audio:{sound:true,music:false,effects:true,volume:0.35},favorites:[],recent:[],best:{},runs:[],setLanguage:language=>set({language}),setTheme:theme=>set({theme}),setAudio:v=>set(s=>({audio:{...s.audio,...v}})),toggleFavorite:id=>set(s=>({favorites:s.favorites.includes(id)?s.favorites.filter(x=>x!==id):[...s.favorites,id]})),visit:id=>set(s=>({recent:[id,...s.recent.filter(x=>x!==id)].slice(0,12)})),finish:run=>set(s=>({best:{...s.best,[run.game]:Math.max(s.best[run.game]||0,run.score)},runs:[run,...s.runs].slice(0,300)}))}),{name:'bobby-portal',version:1}));
export const achievementRules=[{id:'firstPlay',test:(runs:Run[])=>runs.length>0},{id:'explorer',test:(runs:Run[])=>new Set(runs.map(r=>r.game)).size===3},{id:'mangoMaster',test:(runs:Run[])=>runs.some(r=>r.game==='mango-catch'&&r.score>=100)},{id:'towerMaster',test:(runs:Run[])=>runs.some(r=>r.game==='temple-tower'&&r.score>=10)},{id:'rushMaster',test:(runs:Run[])=>runs.some(r=>r.game==='tuk-tuk-rush'&&r.score>=500)}];
